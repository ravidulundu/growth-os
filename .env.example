# -----------------------------
# Runtime
# -----------------------------
# Node runtime mode for local development.
NODE_ENV=development
# Web dev port (change if 3000 is occupied).
WEB_PORT=3000
# Public web application URL used to build callback links.
APP_URL=http://localhost:3000
# API base URL used by clients and internal links.
API_URL=http://localhost:4000
# Auth endpoint base URL used by Better Auth link generation.
BETTER_AUTH_BASE_URL=http://localhost:4000
# Public API URL exposed to Next.js browser runtime.
NEXT_PUBLIC_API_URL=http://localhost:4000
# API listen port.
API_PORT=4000
# Comma-separated CORS allowlist for browser origins (optional).
# If empty, APP_URL is used; for local fallback localhost origins are allowed.
CORS_ALLOWED_ORIGINS=

# -----------------------------
# Application Auth
# -----------------------------
# Local auth mode: magic_link (default), oauth in future.
AUTH_PROVIDER=magic_link
# Session/JWT signing secret for app auth.
JWT_SECRET=replace_me
# Better Auth secret (recommended to set explicitly in all environments).
BETTER_AUTH_SECRET=replace_me
# Max magic-link requests per email in 1 hour window.
AUTH_MAGIC_LINK_MAX_REQUESTS_PER_HOUR=5
# In dev/test with SMTP disabled, print full magic-link URL in logs.
# Keep false by default to avoid leaking raw tokens into aggregated logs.
AUTH_DEV_LOG_MAGIC_LINK_URL=false
# Force Secure attribute for session cookie. Defaults to true in production.
AUTH_COOKIE_SECURE=
# Sender address for magic-link emails.
# Use onboarding@resend.dev for sandbox testing, or a verified domain sender for real delivery.
MAGIC_LINK_FROM_EMAIL=onboarding@resend.dev

# -----------------------------
# Optional App OAuth Providers
# -----------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# -----------------------------
# X API Integration
# -----------------------------
# X OAuth client credentials from X Developer Portal.
X_CLIENT_ID=
X_CLIENT_SECRET=
# OAuth callback URL for X app.
X_REDIRECT_URI=http://localhost:4000/auth/x/callback
# Minimum scopes for read/write + token refresh capability.
X_SCOPES=tweet.read tweet.write users.read offline.access
# X client mode: mock (dev/test). Do not use mock in production.
X_CLIENT_MODE=mock

# -----------------------------
# Data Stores
# -----------------------------
# Postgres connection string (docker compose local default).
DATABASE_URL=postgres://postgres:postgres@localhost:55432/growth_os
# Redis connection string for queue/cache/rate-limit state.
REDIS_URL=redis://localhost:56379

# -----------------------------
# Crypto
# -----------------------------
# 32-byte key used for token encryption/decryption at app level.
TOKEN_ENCRYPTION_KEY=replace_with_32_byte_key

# -----------------------------
# AI
# -----------------------------
# LLM provider selection: openrouter (default) or stub (local fallback).
LLM_PROVIDER=openrouter
# OpenRouter API key used by generation orchestrator.
OPENROUTER_API_KEY=
# OpenRouter model identifier (example: openai/gpt-4.1-mini).
OPENROUTER_MODEL=openai/gpt-4.1-mini
# OpenRouter API base URL (override only for proxy/self-hosted gateway).
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional app name shown in OpenRouter dashboard/request headers.
OPENROUTER_APP_NAME=growth-os
# Legacy fallback alias kept for compatibility when OPENROUTER_MODEL is empty.
OPENAI_MODEL=

# -----------------------------
# Safety Controls
# -----------------------------
# Require explicit human confirmation before scheduling publish jobs.
SAFE_MODE_ENABLED=true
# Duplicate similarity guard in worker (defaults to SAFE_MODE_ENABLED when unset).
SAFE_MODE_SIMILARITY_GUARD_ENABLED=
# Block publishing when similarity score to recent posts exceeds this threshold.
SAFE_MODE_MAX_SIMILARITY=0.85

# -----------------------------
# Observability
# -----------------------------
# Sentry DSN for backend/frontend error tracking.
SENTRY_DSN=
# Log verbosity: debug | info | warn | error.
LOG_LEVEL=info

# -----------------------------
# SMTP (optional local dev with MailHog)
# -----------------------------
# Leave empty to use dev console fallback for magic links.
# Set SMTP_HOST=localhost when MailHog is running.
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=resend
SMTP_PASS=
