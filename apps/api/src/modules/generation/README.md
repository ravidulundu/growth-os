# generation

Domain module placeholder.

Contract:

- Input DTOs and validation
- Service layer for domain logic
- Controller/handlers (when exposed over HTTP)
- Module-level tests under apps/api/tests/generation
